# Infrastructure notes

This project is a demo Next.js app. Infrastructure considerations:

- For persistence, add a database (SQLite for local, Postgres for production).
- For CI/CD, add GitHub Actions to run lint/test/build and deploy (there is a README section for DevOps).
- The project includes a Dockerfile for running the application in a container.
